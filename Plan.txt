Plan - wersja 2:

Potrzebujemy:
	1. SilnikGraficzny: <moduł> //z natury uniwersalne urządzenie
		-obsługuje ncurses
		-uruchamia funkcję on_tick(delta_time,[keys_pressed])
		-ma funkcję draw(character,color,x,y) --> w przypadku displayable, x,y są przesunięte w stosunku do orginału.
		-przyjmuje obiekty "displayable"
			//displayable.draw() //uruchamiana co tick jeśli displayable istnieje w SilnikuGraficznym
			//displayable.draw_character() //powinna być umieszczona wewnątrz draw()
			//displayable.coords == (int x,int y)
		-pozwala na usuwanie/dodawanie tych obiektów. obiekty są rysowane aż nie zostaną usunięte z silnika.

	2. SilnikGry <moduł>
		- używa SilnikaEventów i SilnikaModułów.

	3. SilnikEventów <moduł> //z natury uniwersalny
		-umożliwia dodawanie i usuwanie eventów
		-ma funkcją resolve(czas) która uruchamia eventy które mogą zdarzyć się od ostatniego czasu.



[259 up, 258 down][260 left, 261 right]
